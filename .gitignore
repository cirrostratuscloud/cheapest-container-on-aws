# Terraform / OpenTofu
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Note: .terraform.lock.hcl IS committed on purpose (pins provider versions).
